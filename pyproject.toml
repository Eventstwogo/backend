[tool.black]
line-length = 80
target-version = ['py310']

[tool.isort]
profile = "black"
line_length = 80

[tool.mypy]
strict = true
ignore_missing_imports = true
exclude = ["venv/"]


[[tool.mypy.overrides]]
module = ["fastapi.*", "pydantic.*"]
ignore_missing_imports = true
